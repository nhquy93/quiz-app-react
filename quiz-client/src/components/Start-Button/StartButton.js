import React from "react";
import { Button } from "antd";
import "./StartButton.css";

export default function StartButton() {
  return <Button className="start" type="primary">Start Quiz</Button>;
}
